1. run_document_conversion_consumer超时，加上生成图片描述以后，Kafka默认的5分钟大概率不够，连接失效了以后，提交不了offset，可能导致重复消费等问题。
2. 生成图片描述太慢了，我的端到端测试，花了半个小时
3. 图片minio url可见性问题，现在是设置minio能匿名访问，发给llm还好说，因为prompt的组装在后端完成，我们可以给llm发base64的图片编码，但生产想预览得改成后端图片代理或 presigned URL。